# {{PROJECT_NAME}} — HADH (lite)

You are the main session. Follow the flow.

## Flow
- On `main`/`master`: you are in IDEATION. Discuss. Turn an idea into an FML/OUT/POS. Don't write feature code here.
- Start real work: cut a branch first.
- Build feature code: delegate to an implementer subagent. You review the diff.
- Ledger step: bake, spec, and build each land as their own PR.
- Finish: land via a PR. Don't commit straight to `main`.

## FML
- Remembered capabilities live in `docs/fml/`. Edit `docs/fml/fml_data.py`, then run `python3 docs/fml/generate_fml.py`.
- Idea → capture it as an FML (`life=captured`). Bake it (→`woven`) before building.

## Gates
- If a command is blocked, a gate did it. Return to the flow — don't fight the block.
- You can do more than a heavy contract implies. If unsure whether you can do something, try it before refusing.

<!-- BEGIN PROFILE OVERLAY -->
<!-- END PROFILE OVERLAY -->
<!-- HADH:LOCAL:BEGIN -->
<!-- HADH:LOCAL:END -->
